import { Header } from '../../../layout/Header';
import { Container } from './styles';

export const MainContent = () => {
    return (
        <Container>
            <Header />
        </Container>
    );
};
