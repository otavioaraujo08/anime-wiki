import { AnimeList } from './AnimeList';
import { MainContent } from './MainContent';

export const Home = () => {
    return (
        <>
            <MainContent />
            <AnimeList />
        </>
    );
};
